Invoke the push-standards skill. Extract coding patterns and conventions from the current codebase and append them to the Project-Specific Patterns section of .claude/CODING-STANDARDS.md. Forge defaults at the top of the file are never modified. Each standard needs a rule, reason, and concrete example.

Skill: global/.claude/skills/push-standards/SKILL.md
