Invoke the ingest skill. Compile unprocessed Raw/ items into the Wiki. Handles three intake modes — files already in Raw/, uploaded files, and pasted text. Defaults to the current system context; use --all to process all systems.

Skill: global/.claude/skills/ingest/SKILL.md
