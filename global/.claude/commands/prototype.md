Invoke the prototype skill. Name the design question, then pick a branch — a logic/state question builds an interactive Logic Prototype (pure liftable logic module behind a throwaway harness, per logic-prototype.md); a visual/UX question builds 3–5 structurally different UI variants behind a dev-only switcher (per ui-prototype.md). Spike code lives under /prototype, never src/, and nothing carries over silently. Findings feed /write-prd, which preserves the spike on a throwaway branch before cleaning the working tree.

Skill: global/.claude/skills/prototype/SKILL.md
