Invoke the tdd skill. Build a feature or fix a bug using test-driven development — red-green-refactor, vertical slices, one test at a time. Tests verify behaviour through public interfaces, not implementation details. Reference the testplan for what to test.

Skill: global/.claude/skills/tdd/SKILL.md
