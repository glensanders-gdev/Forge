Invoke the setup-confluence skill. Interactively configure Confluence publishing for the knowledge base — walks through connection details, auth method, and optional plugin settings, then validates the connection before writing confluence.md.

Skill: global/.claude/skills/setup-confluence/SKILL.md
