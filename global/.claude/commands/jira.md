Invoke the jira skill. Interact with Jira tickets — fetch and analyse requirements/AC/test scenarios (AFK), preview and post progress comments (HITL), transition ticket status (HITL), or run JQL searches. Subcommands: get, comment, transition, search, setup. Requires Jira MCP server or JIRA_URL/JIRA_EMAIL/JIRA_API_TOKEN env vars.

Skill: global/.claude/skills/jira/SKILL.md
