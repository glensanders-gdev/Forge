Invoke the to-tickets skill. Convert an agreed plan, PRD, spec, or conversation into a set of vertical-slice kanban tickets — each a tracer bullet (a narrow but complete path through every layer) sized to the smart zone, with genuine blocking edges and HITL/AFK tags. Quizzes you on granularity and blocking edges before writing to docs/kanban.md in dependency order. Wide refactors use expand–contract; oversized slices delegate to /break-down. Bridges /write-prd → /build.

Skill: global/.claude/skills/to-tickets/SKILL.md
