Invoke the company-git skill. Connect the local company knowledge directory to a company-approved GitHub remote. One-time setup — initialises a git repo in ~/.claude/companies/[name]/, adds the remote, and either pushes initial stubs or pulls existing team content. Run after /company-add.

Skill: global/.claude/skills/company-git/SKILL.md
