Bootstrap Forge on a new machine. Clones https://github.com/glensanders-gdev/Forge to ~/forge and runs install.sh. Use when ~/.claude/skills/ is missing or empty. Redirects to /forge-update if Forge is already installed.