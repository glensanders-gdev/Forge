Invoke the seo skill. Structured SEO audit and remediation planning — reads source files directly, ranks findings by severity (Critical/High/Medium), confirms scope, writes a dated report to docs/seo/, and optionally creates kanban tickets. Covers technical SEO (crawlability, canonicals, redirects), on-page (titles, meta, headings), structured data (JSON-LD), Core Web Vitals, and content quality. Flags: --audit, --page <path>, --schema, --vitals, --content, --analyze-only.
