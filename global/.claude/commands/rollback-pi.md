Invoke the rollback-pi skill. Roll back all projects in the current PI release in reverse deploy order (last deployed first). Captures one reason for all projects. Each project requires individual ROLLBACK [version] confirmation. Stops entire sequence on any failure. Updates PI plan with accurate partial or full rollback state.

Skill: global/.claude/skills/rollback-pi/SKILL.md
