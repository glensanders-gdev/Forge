Invoke the dependency-update skill. Update project dependencies safely. --safe (default) handles patch and minor updates; --all includes major version bumps with per-package confirmation showing breaking changes. Runs tests after updating and stages changes for review — never auto-commits.

Skill: global/.claude/skills/dependency-update/SKILL.md
