# Context Health Report

**Project:** Forge (framework repo)
**Generated:** 2026-06-11
**Previous report:** First check

---

## Session Budget

| Layer | Tokens | % of 100k |
|-------|--------|-----------|
| Auto-loaded (every session) | 8,070 | 8% |
| On-demand (PRD, knowledge, skills) | 0 fixed (skills variable — see below) | 0% |
| **Estimated session load** | **~8,070** | **~8%** |
| Remaining for conversation | ~91,930 | ~92% |

**Overall status:** ✅ Green

Note: this repo injects `~/.claude/rules/common/` (7 files, 3,526 tokens) into every session via global instructions — included in the auto-loaded total above even though the standard checklist doesn't list it. On-demand load here is dominated by skill invocations (1–8.8k tokens each), which vary per session.

---

## File Breakdown

### 🔴 Red — Immediate Action Required

| File | Tokens | Threshold | Action |
|------|--------|-----------|--------|
| 20 skills exceed the single-skill red threshold | 2,076–8,765 each | 2,000 | See `docs/skill-size-audit.md` (2026-06-11) — top offenders: company-add (8,765), graphify (8,486), dashboard-tokens (8,375), fy-review (5,123). Recommended fix: move embedded templates to `references/` folders. |

### ⚠️ Amber — Monitor or Trim

| File | Tokens | Threshold | Action |
|------|--------|-----------|--------|
| docs/kanban.md | 1,080 | 800 | 31 of 39 tickets are Done — archive them to `docs/kanban-archive.md` (happens automatically at `/sprint-end`). Estimated saving: ~800 tokens. |

### ✅ Green — Within Budget

| File | Tokens |
|------|--------|
| CLAUDE.md (project) | 943 |
| ~/.claude/PRINCIPLES.md | 1,508 (framework file — awareness only) |
| ~/.claude/SOUL.md | 628 (framework file — awareness only) |
| ~/.claude/rules/ (7 injected files) | 3,526 |
| docs/HANDOFF.md | 176 |
| docs/DEVLOG.md | 78 |
| ~/.claude/preferences.md | 131 |

**Missing (not errors):** `docs/CONTEXT.md`, `~/.claude/knowledge/company/acronyms.md`, `~/.claude/knowledge/company/context.md`, `~/.claude/knowledge/CLAUDE.md`. No active PRD in `docs/prd/active/`. No ADRs in `docs/adr/`. No system or project knowledge folders. No active company set.

---

## Trend

| Date | Auto-load | On-demand | Total | Status |
|------|-----------|-----------|-------|--------|
| 2026-06-11 | 8,070 | 0 (+skills) | ~8,070 | ✅ |
| — | — | — | — | First check |

---

## Recommended Actions

1. `docs/kanban.md` (1,080 tokens ⚠️) — archive the 31 Done tickets to `docs/kanban-archive.md` under a sprint heading. Estimated saving: ~800 tokens per session start. Happens automatically at next `/sprint-end`.
2. Skill slimming (🔴, recurring per-invocation cost) — implement recommendation 1 from `docs/skill-size-audit.md`: extract dashboard-tokens' embedded HTML template to `references/dashboard-template.html`. Estimated saving: ~5–6k tokens per invocation of a frequently-run skill.
3. Consider a `rules/` size watch — the injected `~/.claude/rules/common/` set (3,526 tokens) grows with every new rule file and loads in *every* session across *all* projects. Currently fine; flag again if it passes ~5k.

---

## Child Node Recommendations

No source directories detected (looked for src/, app/, lib/, packages/, services/, api/, components/). This repo's "source" is `global/.claude/skills/` — its per-file audit lives in `docs/skill-size-audit.md`.

---

*Generated by /context-health — read-only audit, no files were modified.*
